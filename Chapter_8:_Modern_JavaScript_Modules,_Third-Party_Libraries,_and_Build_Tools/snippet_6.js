// Importing Lodash (using ES6 modules syntax, if supported)
import _ from 'lodash';
const numbers = [10, 5, 8, 1];
const maxNumber = _.max(numbers);
console.log("Max number:", maxNumber); // Output: 10